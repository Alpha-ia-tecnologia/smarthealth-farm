# Build é feito dentro da imagem — nada de artefatos/locais no contexto.
node_modules
dist
dist-ssr
coverage

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Variáveis de ambiente locais (a URL da API entra via build arg VITE_API_URL).
.env
.env.*
!.env.example

# Git / editor / SO
.git
.gitignore
.vscode
.idea
.DS_Store

# Docker / docs que não afetam o build
Dockerfile
.dockerignore
*.md
